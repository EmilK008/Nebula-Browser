# Copy to .evs-credentials in the project root (gitignored).
# Used by npm run dist / evs:reauth when the EVS password prompt cannot accept input.
#
#   copy .evs-credentials.example .evs-credentials
#
EVS_ACCOUNT_NAME=your_evs_account_name
EVS_PASSWD=your_evs_password
